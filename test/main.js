var imageHelper = require('../');
var assert = require('assert');

describe('gulp-compass-imagehelper', function() {

  before(function(done) {
    done();
  });

  after(function(done) {
    done();
  });

  describe('imageHelper()', function() {
    it('should throw, when path is missing', function() {
      assert.equal(true, false);
    });

    it('should log notice if svg dimensions not available', function() {
      assert.equal(true, false);
    });
  });

});